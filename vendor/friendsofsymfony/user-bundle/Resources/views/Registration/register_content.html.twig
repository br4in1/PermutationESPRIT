{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, {'method': 'post', 'action': path('fos_user_registration_register'), 'attr': {'class': 'fos_user_registration_register register'}}) }}

    <p class="form-row form-row-wide">
        <label for="username2">
            <i class="im im-icon-Mail"></i>
            {{ form_widget(form.email) }}
            <div class="notification error closeable">{{ form_errors(form.email) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Male"></i>
            {{ form_widget(form.firstname) }}
            <div class="notification error closeable">{{ form_errors(form.firstname) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Male"></i>
            {{ form_widget(form.lastname) }}
            <div class="notification error closeable">{{ form_errors(form.lastname) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Lock-2"></i>
            {{ form_widget(form.plainPassword.first) }}
            <div class="notification error closeable">{{ form_errors(form.plainPassword.first) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Lock-2"></i>
            {{ form_widget(form.plainPassword.second) }}
            <div class="notification error closeable">{{ form_errors(form.plainPassword.second) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Phone"></i>
            {{ form_widget(form.phone) }}
            <div class="notification error closeable">{{ form_errors(form.phone) }}</div>
        </label>
    </p>
    <p class="form-row form-row-wide">
        <label>
            <i class="im im-icon-Book"></i>
            {{ form_widget(form.specialite) }}
            <div class="notification error closeable">{{ form_errors(form.specialite) }}</div>
        </label>
    </p>
    <button type="submit" class="button border fw margin-top-10">{{ 'registration.submit'|trans }}</button>
{{ form_end(form) }}
